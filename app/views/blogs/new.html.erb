<h1>Create a new blog</h1>

<%= form_for @blog do |f| %>
	<%= f.hidden_field :user_id, :value => @user.id %>
	<%= f.text_field :title %>
	<%= f.text_field :category %>
	<%= f.text_area :content %>
	<%= f.submit %>
<% end %>
